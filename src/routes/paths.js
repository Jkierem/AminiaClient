const BASE = process.env.PUBLIC_URL;

export const MENU_PATH = `${BASE}/menu`;
export const GAME_PATH = `${BASE}/game`;
export const LOGIN_PATH = `${BASE}/login`;
export const SAVES_PATH = `${BASE}/saves`;
